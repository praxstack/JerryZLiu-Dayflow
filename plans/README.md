# Implementation Plans

Generated by the improve skill on 2026-08-29. **Non-interactive default:** top 5 findings by leverage were planned automatically (user requested full `/improve` run in background).

**Planned at:** commit `274c059` (main, 2026-08-29)

**Audit scope:** Dayflow repo only (`/workspace`, github.com/praxstack/jerryzliu-dayflow). No CONTEXT.md or ADRs present. Recent hot spots weighted: dayflow-cli/MCP, agent bridge, Flow subsystem, LLM providers.

**Not audited:** Full UI view layer (342 Swift files), Sparkle release scripts, super-pro skill stack (global install only), production macOS runtime behavior.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Add CI verification baseline | P1 | M | — | TODO |
| 002 | Extract shared Dayflow calendar kernel | P1 | M | 001 | TODO |
| 003 | Add dayflow-cli test suite | P1 | M | 001 | TODO |
| 004 | Add Flow subsystem characterization tests | P2 | M | 001 | TODO |
| 005 | Add AGENTS.md for agent contributors | P2 | S | — | TODO |

## Dependency notes

- **002** and **003** should land after **001** so regressions in shared date logic or CLI parsing are caught automatically.
- **004** benefits from **001** but can proceed in parallel once CI exists.

## Findings summary (vetted, by leverage)

| # | Finding | Category | Impact | Effort | Risk | Evidence |
|---|---------|----------|--------|--------|------|----------|
| 1 | No CI pipeline — tests exist but never run on push/PR | dx / tests | Every merge is unverified; CLI and app tests rot silently | M | LOW | `.github/` contains only `ISSUE_TEMPLATE/` — no workflows |
| 2 | Duplicated 4 AM day/week + category logic between CLI and app | tech-debt | Agent/CLI timeline queries can drift from app UI day boundaries | M | MED | `tools/dayflow-cli/Sources/dayflow/DayBoundary.swift:7-8`, `Categories.swift:9`, `StorageDateHelpers.swift:22-39` |
| 3 | Zero automated tests for bundled dayflow-cli / MCP server | tests | Write tools and JSON-RPC parsing ship without regression safety | M | LOW | `tools/dayflow-cli/` — no test target; `Package.swift:14-17` executable only |
| 4 | Flow subsystem (recent hot spot) has no unit tests | tests | Distraction agent + session mirror churn without safety net | M | LOW | `Core/Flow/*.swift` — no matching `*Tests.swift`; git log shows Flow commits since `b3b38c9` |
| 5 | Missing AGENTS.md for agent-heavy repo | dx | Cloud Agents and MCP contributors lack build/test/convention entry point | S | LOW | No `AGENTS.md`/`CLAUDE.md` at repo root; `.agents/README.md` covers skill install only |

## Direction (not ranked against bugs)

- **Expand CLI read parity:** MCP already exposes timeline queries; README promises export/chat — CLI could become the canonical headless interface if the calendar kernel is shared (see plan 002).
- **Deepen LLMService:** 1,309-line module mixes provider routing, batch processing, and chat streaming — worth a design spike after verification baseline (not planned here; see architecture HTML report candidate #3).

## Findings considered and rejected

- **Agent bridge lacks auth beyond Unix socket permissions:** By-design for local-only macOS tool (`AgentBridgeServer.swift:86-87` chmod 0600); not a finding unless remote access is added.
- **Timelapse generation swallows errors:** Real but low leverage (`AnalysisManager.swift:594-596` prints only) — defer until CI baseline exists.
- **LLMService god module refactor:** Valid architecture concern but Effort L with high blast radius; architecture report covers it; not in top-5 execution queue.
